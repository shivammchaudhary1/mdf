import "reflect-metadata";import { validate } from "class-validator";import { ProfileDto } from "../src/modules/profiles/profile.dto";describe("profile portfolio limit",()=>{it("rejects a ninth photo",async()=>{const dto=Object.assign(new ProfileDto(),{portfolioMediaIds:Array.from({length:9},(_,i)=>`64b64c00000000000000000${i}`)});expect((await validate(dto)).some(e=>e.property==="portfolioMediaIds")).toBe(true);});});
