import { BadRequestException,ConflictException,Injectable,NotFoundException } from "@nestjs/common"; import { InjectModel } from "@nestjs/mongoose"; import { Types,type Model,type PipelineStage } from "mongoose"; import { AuditService } from "../../common/audit/audit.service"; import { pageMeta } from "../../common/dto/pagination.dto"; import { RateLimitService } from "../../common/security/rate-limit.service"; import { objectId } from "../../common/utils/object-id"; import { escapeSearch } from "../../common/utils/search"; import { Account } from "../auth/auth.models"; import { Casting } from "../castings/casting.model"; import { MailService } from "../mail/mail.service"; import { MediaService } from "../media/media.service"; import { Project } from "../projects/project.model"; import { Application } from "./application.model"; import { AdminApplicationQueryDto,CreateApplicationDto,MemberApplicationQueryDto,OpportunityQueryDto,UpdateApplicationDto } from "./application.dto";
type ResolvedOpportunity={type:"PROJECT"|"CASTING";id:Types.ObjectId;title:string;slug?:string;role?:string;projectId?:Types.ObjectId};
@Injectable()export class ApplicationService{constructor(@InjectModel("Application")private readonly applications:Model<Application>,@InjectModel("Account")private readonly accounts:Model<Account>,@InjectModel("Project")private readonly projects:Model<Project>,@InjectModel("Casting")private readonly castings:Model<Casting>,private readonly media:MediaService,private readonly mail:MailService,private readonly audit:AuditService,private readonly rateLimits:RateLimitService){}
 private serialize(a:Application|Record<string,unknown>){const i=a as Application;const p=(i.portfolioMediaIds??[]).map(String);const d=i.documentMediaId?String(i.documentMediaId):undefined;return{...a,_id:String((a as{_id:unknown})._id),userId:String(i.userId),opportunityId:String(i.opportunityId),projectId:i.projectId?String(i.projectId):undefined,portfolioMediaIds:p,portfolioImages:p.map(id=>this.media.urlsFor(id).medium),documentMediaId:d,documentUrl:d?this.media.urlsFor(d,"document").document:undefined}}
 private async resolveOpportunity(input:CreateApplicationDto):Promise<ResolvedOpportunity>{const id=objectId(input.opportunityId,"Opportunity not found.");if(!input.opportunityType||input.opportunityType==="CASTING"){const c=await this.castings.findOne({_id:id,published:true,archived:false,status:"Open",$or:[{deadline:{$exists:false}},{deadline:null},{deadline:{$gt:new Date()}}]}).lean();if(c)return{type:"CASTING",id:c._id,title:c.title,slug:c.slug,role:c.role,projectId:c.projectId}}if(!input.opportunityType||input.opportunityType==="PROJECT"){const p=await this.projects.findOne({_id:id,published:true,archived:false,status:{$nin:["Completed","Archived"]}}).lean();if(p)return{type:"PROJECT",id:p._id,title:p.title,slug:p.slug,projectId:p._id}}throw new BadRequestException("This opportunity is no longer accepting applications.")}
 async apply(userId:string,input:CreateApplicationDto){await this.rateLimits.consume("member-application",userId,30,86400000);const o=await this.resolveOpportunity(input);await this.media.assertOwnedBy(userId,[input.documentMediaId,...(input.portfolioMediaIds??[])]);const acc=await this.accounts.findById(objectId(userId)).lean();if(!acc||acc.suspended)throw new BadRequestException("Account is not available.");try{const a=await this.applications.create({userId:acc._id,opportunityType:o.type,opportunityId:o.id,projectId:o.projectId,opportunityTitle:o.title,opportunitySlug:o.slug,roleSnapshot:o.role,applicant:{name:acc.name,email:acc.email,mobile:acc.mobile},coverNote:input.coverNote,portfolioMediaIds:input.portfolioMediaIds?.map(id=>new Types.ObjectId(id)),showreelUrl:input.showreelUrl,pitch:input.pitch,documentMediaId:input.documentMediaId?new Types.ObjectId(input.documentMediaId):undefined,status:"Submitted"});await this.mail.send(acc.email,"Application received",`Your application for ${o.title} has been received.`).catch(()=>undefined);return this.serialize(a.toObject())}catch(error:unknown){if(error&&typeof error==="object"&&"code"in error&&error.code===11000)throw new ConflictException("You have already applied to this opportunity.");throw error}}
 async mine(userId:string,q:MemberApplicationQueryDto){const f={userId:objectId(userId),...(q.status?{status:q.status}:{})};const[items,total]=await Promise.all([this.applications.find(f).sort({createdAt:-1}).skip((q.page-1)*q.limit).limit(q.limit).lean(),this.applications.countDocuments(f)]);return{items:items.map(v=>this.serialize(v)),meta:pageMeta(q.page,q.limit,total)}}
 async mineById(userId:string,id:string){const a=await this.applications.findOne({_id:objectId(id),userId:objectId(userId)}).lean();if(!a)throw new NotFoundException("Application not found.");return this.serialize(a)}
 async adminList(q:AdminApplicationQueryDto){const f:Record<string,unknown>={...(q.status?{status:q.status}:{}),...(q.opportunityType?{opportunityType:q.opportunityType}:{}),...(q.opportunityId?{opportunityId:objectId(q.opportunityId)}:{}),...(q.projectId?{projectId:objectId(q.projectId)}:{})};if(q.search){const s=new RegExp(escapeSearch(q.search.trim()),"i");f.$or=[{"applicant.name":s},{"applicant.email":s},{opportunityTitle:s},{roleSnapshot:s}]}const[items,total]=await Promise.all([this.applications.find(f).select("+adminNotes").sort({createdAt:-1}).skip((q.page-1)*q.limit).limit(q.limit).lean(),this.applications.countDocuments(f)]);return{items:items.map(v=>this.serialize(v)),meta:pageMeta(q.page,q.limit,total)}}
 async adminById(id:string){const a=await this.applications.findById(objectId(id)).select("+adminNotes").lean();if(!a)throw new NotFoundException("Application not found.");return this.serialize(a)}
 async update(id:string,input:UpdateApplicationDto,actorId:string){const a=await this.applications.findByIdAndUpdate(objectId(id),{$set:{status:input.status,...(input.adminNotes!==undefined?{adminNotes:input.adminNotes}:{}),reviewedBy:objectId(actorId),reviewedAt:new Date()}},{new:true,runValidators:true}).select("+adminNotes");if(!a)throw new NotFoundException("Application not found.");await this.mail.send(a.applicant.email,"Application status updated",`Your application for ${a.opportunityTitle} is now ${a.status}.`).catch(()=>undefined);await this.audit.record({actorId,action:"application.update",entityType:"application",entityId:id,summary:`${a.applicant.name} → ${a.status}`,metadata:{status:a.status,opportunityType:a.opportunityType}});return this.serialize(a.toObject())}
 async opportunities(q:OpportunityQueryDto){const cm:Record<string,unknown>={published:true,archived:false,status:"Open",$or:[{deadline:{$exists:false}},{deadline:null},{deadline:{$gt:new Date()}}]};const pm:Record<string,unknown>={published:true,archived:false,status:{$nin:["Completed","Archived"]}};if(q.category){cm.category=q.category;pm.type=q.category}if(q.location){const l=new RegExp(escapeSearch(q.location.trim()),"i");cm.location=l;pm.location=l}if(q.search){const s=new RegExp(escapeSearch(q.search.trim()),"i");cm.$and=[{$or:[{title:s},{role:s},{summary:s}]}];pm.$or=[{title:s},{summary:s}]}const pipeline:PipelineStage[]=[{$match:cm},{$project:{_id:1,title:1,slug:1,category:1,role:1,location:1,status:1,deadline:1,coverMediaId:1,createdAt:1,opportunityType:{$literal:"CASTING"}}},{$unionWith:{coll:this.projects.collection.name,pipeline:[{$match:pm},{$project:{_id:1,title:1,slug:1,category:"$type",location:1,status:1,coverMediaId:1,createdAt:1,opportunityType:{$literal:"PROJECT"}}}]}}];const[items,count]=await Promise.all([this.castings.aggregate([...pipeline,{$sort:{createdAt:-1}},{$skip:(q.page-1)*q.limit},{$limit:q.limit}]),this.castings.aggregate([...pipeline,{$count:"total"}])]);const total=Number(count[0]?.total??0);return{items:items.map(item=>{const c=item.coverMediaId?String(item.coverMediaId):undefined;return{...item,_id:String(item._id),coverMediaId:c,coverImage:c?this.media.urlsFor(c).medium:undefined}}),meta:pageMeta(q.page,q.limit,total)}}
}
