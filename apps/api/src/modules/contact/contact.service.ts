import{Injectable,NotFoundException}from"@nestjs/common";import{ConfigService}from"@nestjs/config";import{InjectModel}from"@nestjs/mongoose";import type{Model}from"mongoose";import{AuditService}from"../../common/audit/audit.service";import{pageMeta}from"../../common/dto/pagination.dto";import{sha256}from"../../common/utils/crypto";import{objectId}from"../../common/utils/object-id";import{escapeSearch}from"../../common/utils/search";import{MailService}from"../mail/mail.service";import{ContactMessage}from"./contact.model";import{ContactDto,ContactQueryDto,ContactUpdateDto}from"./contact.dto";
@Injectable()export class ContactService{constructor(@InjectModel("Contact")private readonly contacts:Model<ContactMessage>,private readonly mail:MailService,private readonly config:ConfigService,private readonly audit:AuditService){}async create(input:ContactDto,ip?:string){const pepper=String(this.config.get("COOKIE_SECRET")??"development");await this.contacts.create({...input,email:input.email.trim().toLowerCase(),status:"New",ipHash:ip?sha256(`${pepper}:${ip}`):undefined});const recipient=this.config.get<string>("CONTACT_EMAIL");if(recipient)await this.mail.send(recipient,`Contact: ${input.subject}`,`${input.name} (${input.email})\n\n${input.message}`).catch(()=>undefined);return{message:"Your message has been received."}}async list(q:ContactQueryDto){const f:Record<string,unknown>={...(q.status?{status:q.status}:{})};if(q.search){const s=new RegExp(escapeSearch(q.search.trim()),"i");f.$or=[{name:s},{email:s},{subject:s}]}const[items,total]=await Promise.all([this.contacts.find(f).sort({createdAt:-1}).skip((q.page-1)*q.limit).limit(q.limit).lean(),this.contacts.countDocuments(f)]);return{items:items.map(x=>({...x,_id:String(x._id)})),meta:pageMeta(q.page,q.limit,total)}}async detail(id:string){const x=await this.contacts.findById(objectId(id)).lean();if(!x)throw new NotFoundException("Contact query not found.");return{...x,_id:String(x._id)}}async update(id:string,input:ContactUpdateDto,actorId:string){const x=await this.contacts.findByIdAndUpdate(objectId(id),{$set:{status:input.status}},{new:true,runValidators:true});if(!x)throw new NotFoundException("Contact query not found.");await this.audit.record({actorId,action:"contact.update",entityType:"contact",entityId:id,summary:x.subject,metadata:{status:x.status}});return{...x.toObject(),_id:String(x._id)}}async remove(id:string,actorId:string){const x=await this.contacts.findByIdAndDelete(objectId(id));if(!x)throw new NotFoundException("Contact query not found.");await this.audit.record({actorId,action:"contact.delete",entityType:"contact",entityId:id,summary:x.subject});return{message:"Contact query deleted."}}}
