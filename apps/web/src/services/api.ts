import{runtimeConfig}from"@/config/runtime";export class ApiError extends Error{constructor(message:string,public status:number,public requestId?:string){super(message)}}let csrfToken:string|null=null;let csrfPromise:Promise<string|null>|null=null;const SAFE=new Set(["GET","HEAD","OPTIONS"]);const EXEMPT=new Set(["/auth/login","/auth/google","/auth/register","/auth/forgot-password","/auth/reset-password","/contact"]);async function loadCsrf(){if(csrfToken)return csrfToken;if(csrfPromise)return csrfPromise;csrfPromise=fetch(`${runtimeConfig.apiUrl}/auth/csrf`,{credentials:"include",cache:"no-store"}).then(async r=>{if(!r.ok)return null;const d=await r.json().catch(()=>null);csrfToken=typeof d?.csrfToken==="string"?d.csrfToken:null;return csrfToken}).finally(()=>{csrfPromise=null});return csrfPromise}export function clearApiSecurityState(){csrfToken=null;csrfPromise=null}export async function api<T>(path:string,options:RequestInit={}):Promise<T>{const method=String(options.method??"GET").toUpperCase();const headers=new Headers(options.headers);if(!(options.body instanceof FormData)&&options.body!==undefined&&!headers.has("Content-Type"))headers.set("Content-Type","application/json");if(!SAFE.has(method)&&!EXEMPT.has(path)){const token=await loadCsrf();if(token)headers.set("X-CSRF-Token",token)}const response=await fetch(`${runtimeConfig.apiUrl}${path}`,{...options,method,credentials:"include",headers,cache:"no-store"});const data=await response.json().catch(()=>null);if(data&&typeof data==="object"&&typeof data.csrfToken==="string")csrfToken=data.csrfToken;if(response.status===401||(path==="/auth/logout"&&response.ok))clearApiSecurityState();if(!response.ok){const message=data?.message;throw new ApiError(Array.isArray(message)?message.join(" "):typeof message==="string"?message:"Something went wrong. Please try again.",response.status,response.headers.get("x-request-id")??data?.requestId)}return data as T}export type CurrentUser={id:string;name:string;email:string;mobile:string;role:"USER"|"SUPER_ADMIN";verified:boolean;csrfToken?:string};
